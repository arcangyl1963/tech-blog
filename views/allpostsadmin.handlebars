<div class="blog-posts list-group">
    {{#each posts}}
    <a class="list-item" href="/dashboard/edit/{{id}}">
        <span>{{title}}</span>
        <span>{{format_date createdOn}}</span>
    </a>
    {{/each}}
</div>